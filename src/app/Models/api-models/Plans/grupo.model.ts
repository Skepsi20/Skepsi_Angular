export interface grupo {
  id: string,
  code: string,
  planId: string,
  groupOfStudents: string,
  tutorId: string
}

export interface newGroup{
  code: string,
  planId: string,
  tutorId: string
}
